version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - MONGODB_URI=mongodb+srv://hasan:10231023@cluster0.qjygrev.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=your-super-secret-jwt-key-here
      - JWT_EXPIRES_IN=7d
      - FILE_UPLOAD_PATH=./uploads
      - MAX_FILE_SIZE=5242880
    volumes:
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - hamkar-network

  # MongoDB Atlas is used instead of local MongoDB
  # mongo-express:
  #   image: mongo-express:latest
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     - ME_CONFIG_MONGODB_SERVER=cluster0-shard-00-00.qjygrev.mongodb.net
  #     - ME_CONFIG_MONGODB_PORT=27017
  #     - ME_CONFIG_MONGODB_ADMINUSERNAME=hasan
  #     - ME_CONFIG_MONGODB_ADMINPASSWORD=10231023
  #     - ME_CONFIG_MONGODB_URL=mongodb+srv://hasan:10231023@cluster0.qjygrev.mongodb.net/
  #     - ME_CONFIG_BASICAUTH_USERNAME=admin
  #     - ME_CONFIG_BASICAUTH_PASSWORD=admin123
  #   restart: unless-stopped
  #   networks:
  #     - hamkar-network

networks:
  hamkar-network:
    driver: bridge
